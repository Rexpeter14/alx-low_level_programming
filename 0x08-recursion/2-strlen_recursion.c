#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - prototype
 * @s: prototype
 * Description: returns length of a string
 * Return: length
 */
int _strlen_recursion(char *s)
{
	int i;

	i = strlen(s);
	return (i);
}
